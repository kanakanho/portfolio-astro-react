---
import styles from "./Tags.module.scss";

type Props = {
  tags: string[];
};

const { tags } = Astro.props;
---

<div class={styles.tagContainer}>
  {
    tags.map((tag) => (
      <a href={`/posts/?tag=${tag}`} class={styles.tag}>
        <div>#&nbsp;{tag}</div>
      </a>
    ))
  }
</div>
